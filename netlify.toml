[build]
  command = "npm run build"
  publish = "dist/dmarmijosa-portafolio/browser"

# SPA fallback so deep links resolve to the Angular app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
